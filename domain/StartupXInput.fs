module StartupXInput 
open System

// type Type = Type of string
// module Type =
//   let create fieldName str :Result<Type, string> =
//     if (String.IsNullOrEmpty(str)) then
//         Error (fieldName + " must be non-empty")
//     else
//         Ok (Type str)
//   let value d = match d with | Type d -> d

// type File = File of string
// module File =
//   let create fieldName str :Result<File, string> =
//     if (String.IsNullOrEmpty(str)) then
//         Error (fieldName + " must be non-empty")
//     else
//         Ok (File str)
//   let value d = match d with | File d -> d