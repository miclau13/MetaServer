module AirPressureInput
open System

// type Kind = Kind of string
// module Kind =
//   let create fieldName str :Result<Kind, string> =
//     if (String.IsNullOrEmpty(str)) then
//         Error (fieldName + " must be non-empty")
//     else
//         Ok (Kind str)
//   let value d = match d with | Kind d -> d

// type File = File of string
// module File =
//   let create fieldName str :Result<File, string> =
//     if (String.IsNullOrEmpty(str)) then
//         Error (fieldName + " must be non-empty")
//     else
//         Ok (File str)
//   let value d = match d with | File d -> d